import{
  colorsLight,
  colorsDark
}
from '../UI/colorsVariables';

const borderRadius = '8px';
const transition = '1';


export const ThemeLight = {
  colors: colorsLight,
  borderRadius,
  transition,
}

export const ThemeDark = {
  colors: colorsDark,
  borderRadius,
  transition,
}

